import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Notes list",
  description: "Notes list description",
};

export default function NoteList() {
  return <div>NotesList page</div>;
}
